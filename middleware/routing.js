export default function({store,redirect,route }) {

  if(route.path != "/"){
    //後で開放
    // if(store.state.auth == "success"){
    //   return redirect(route.path)
    // }
    // // ログインしていなかった場合
    // else{
    //   console.log("ダメです")
    //   return redirect("/");
      
    // }
  }
}